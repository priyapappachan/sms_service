
const config = {
    db : {
        user : 'postgres',
        password : 'admin',
        host : 'localhost',
        port : '5432',
        database : 'postgres',
        dbUrl : 'postgresql://postgres:admin@localhost:5432/postgres'
    }
};

module.exports = config;